<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Criar Usuário</title>
  <link rel="stylesheet" href="/css/login-styles.css">
  <link rel="stylesheet" href="/css/bootstrap-icons.css">
</head>
<body>
  <div class="main-login-wrapper">
    <div class="box">
      <div class="logo-wrapper">
        <img src="/img/itep_logo.png" alt="ITEP" class="login-logo">
        <div class="login-title">Cadastro de Usuário</div>
        <div class="login-subtitle">Preencha os dados para criar um novo usuário</div>
      </div>
      <form action="/auth/register" method="POST" autocomplete="off">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <label for="nome" class="sr-only">Nome</label>
        <div class="input-group input-group-lg">
          <span class="input-group-text"><i class="bi bi-person"></i></span>
          <input type="text" class="form-control" id="nome" name="nome" required placeholder="Nome completo">
        </div>
        <label for="email" class="sr-only">E-mail</label>
        <div class="input-group input-group-lg">
          <span class="input-group-text"><i class="bi bi-envelope"></i></span>
          <input type="email" class="form-control" id="email" name="email" required placeholder="E-mail">
        </div>
        <label for="senha" class="sr-only">Senha</label>
        <div class="input-group input-group-lg">
          <span class="input-group-text"><i class="bi bi-shield-lock"></i></span>
          <input type="password" class="form-control" id="senha" name="senha" required placeholder="Senha">
        </div>
        <label for="perfil" class="sr-only">Perfil</label>
        <div class="input-group input-group-lg">
          <span class="input-group-text"><i class="bi bi-person-badge"></i></span>
          <select class="form-control" id="perfil" name="perfil" required>
            <option value="tecnico">Técnico</option>
            <option value="auditor">Auditor</option>
            <option value="admin">Administrador</option>
          </select>
        </div>
        <button type="submit" class="btn login-btn">
          <i class="bi bi-person-check"></i> Criar Usuário
        </button>
        <% if (error_msg) { %>
          <div class="alert alert-danger"><%= error_msg %></div>
        <% } %>
        <% if (success_msg) { %>
          <div class="alert alert-success"><%= success_msg %></div>
        <% } %>
      </form>
      <div style="text-align:center; margin-top:16px;">
        <a href="/auth/login" style="color:#1976d2; text-decoration:underline; font-size:0.98rem;">Voltar ao login</a>
      </div>
    </div>
  </div>

</body>
</html>
